<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.weather.bigyellowfishtask" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BigYellowFishTask"
        android:usesCleartextTraffic="true" >

        <activity
            android:name=".app.ui.activity.GameActivity"
            android:exported="true"
            android:theme="@style/Theme.BigYellowFishTask.NoActionBar" />
        <activity
            android:name=".app.ui.activity.SurveyActivity"
            android:exported="true"
            android:theme="@style/Theme.BigYellowFishTask.NoActionBar" />
        <activity
            android:name=".app.ui.activity.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.BigYellowFishTask.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>